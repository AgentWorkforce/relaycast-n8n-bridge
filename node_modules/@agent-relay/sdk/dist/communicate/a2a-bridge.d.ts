/**
 * Bridge that connects an external A2A agent into a Relay workspace.
 *
 * - Registers a proxy agent on the Relay workspace
 * - When Relay messages arrive for the proxy, forwards them as A2A JSON-RPC
 *   message/send calls to the external agent
 * - When A2A responses come back, forwards them as Relay DMs
 */
import { Relay } from './core.js';
import { type A2AAgentCard } from './a2a-types.js';
import type { RelayConfig } from './types.js';
export declare class A2ABridge {
    readonly relay: Relay;
    readonly a2aAgentUrl: string;
    readonly proxyName: string;
    private _agentCard?;
    private _started;
    constructor(relayConfig: RelayConfig, a2aAgentUrl: string, proxyName: string);
    start(): Promise<void>;
    stop(): Promise<void>;
    discoverAgent(): Promise<A2AAgentCard>;
    sendA2AMessage(text: string): Promise<string | null>;
    private _handleRelayMessage;
}
//# sourceMappingURL=a2a-bridge.d.ts.map