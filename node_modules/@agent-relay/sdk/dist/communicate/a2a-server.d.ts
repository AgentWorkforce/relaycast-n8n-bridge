/**
 * A2A-compliant HTTP server that exposes a Relay agent as an A2A endpoint.
 *
 * Routes:
 *   GET  /.well-known/agent.json  -> Agent Card
 *   POST /                        -> JSON-RPC 2.0 dispatcher
 */
import { type A2AAgentCard, type A2AMessage, type A2ASkill, type A2ATask } from './a2a-types.js';
export type A2AMessageHandler = (msg: A2AMessage) => A2AMessage | null | Promise<A2AMessage | null>;
export declare class A2AServer {
    readonly agentName: string;
    readonly port: number;
    readonly host: string;
    readonly skills: A2ASkill[];
    readonly tasks: Map<string, A2ATask>;
    private _onMessage?;
    private _server?;
    private _actualPort?;
    constructor(agentName: string, port?: number, host?: string, skills?: A2ASkill[]);
    get url(): string;
    onMessage(callback: A2AMessageHandler): void;
    getAgentCard(): A2AAgentCard;
    handleMessageSend(params: Record<string, unknown>): Promise<Record<string, unknown>>;
    handleTasksGet(taskId: string): Promise<Record<string, unknown>>;
    handleTasksCancel(taskId: string): Promise<Record<string, unknown>>;
    start(): Promise<void>;
    stop(): Promise<void>;
    private _handleRequest;
    private _handleJsonRpc;
}
//# sourceMappingURL=a2a-server.d.ts.map