/**
 * A2A (Agent-to-Agent) protocol transport implementation.
 *
 * Client side: sends JSON-RPC 2.0 to external A2A agent endpoints.
 * Server side: runs a local HTTP server accepting A2A JSON-RPC calls.
 */
import type { Message, MessageCallback } from './types.js';
import { type A2AAgentCard, type A2AConfig, type A2AMessage, type A2ATask } from './a2a-types.js';
export declare class A2AError extends Error {
    readonly code: number;
    constructor(code: number, message: string);
}
export declare class A2ATransport {
    readonly config: A2AConfig;
    agentName?: string;
    agentCard?: A2AAgentCard;
    tasks: Map<string, A2ATask>;
    private _messageCallbacks;
    private _server?;
    private _discoveredCards;
    private _closing;
    constructor(config: A2AConfig);
    register(name: string): Promise<{
        name: string;
        url: string;
        type: string;
    }>;
    unregister(): Promise<void>;
    sendDm(target: string, text: string): Promise<Record<string, unknown>>;
    listAgents(): Promise<Record<string, unknown>[]>;
    onMessage(callback: MessageCallback): void;
    connectWs(): Promise<void>;
    private _handleRequest;
    private _handleAgentCard;
    private _handleJsonRpcHttp;
    private _readBody;
    private _dispatchJsonRpc;
    private _handleMessageSend;
    private _handleTasksGet;
    private _handleTasksCancel;
    private _discoverAgent;
    static _relayMsgToA2A(text: string, _sender: string): A2AMessage;
    static _a2aToRelayMsg(msg: A2AMessage, sender?: string): Message;
    private _a2aResultToRelay;
    private _invokeCallbacks;
    private _authHeaders;
}
//# sourceMappingURL=a2a-transport.d.ts.map