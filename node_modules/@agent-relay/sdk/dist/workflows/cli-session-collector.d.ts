import type { AgentCli } from './types.js';
export interface CliSessionReport {
    cli: AgentCli;
    sessionId: string | null;
    model: string | null;
    provider: string | null;
    durationMs: number | null;
    cost: number | null;
    tokens: {
        input: number;
        output: number;
        cacheRead: number;
    } | null;
    turns: number;
    toolCalls: {
        name: string;
        count: number;
    }[];
    errors: {
        turn: number;
        text: string;
    }[];
    finalStatus: 'completed' | 'failed' | 'unknown';
    summary: string | null;
    raw?: object;
}
export interface CliSessionQuery {
    cli: AgentCli;
    cwd: string;
    startedAt: number;
    completedAt: number;
}
export interface CliSessionCollector {
    canCollect(): boolean;
    collect(query: CliSessionQuery): Promise<CliSessionReport | null>;
}
export declare function createCollector(cli: AgentCli): CliSessionCollector | null;
export declare function collectCliSession(query: CliSessionQuery): Promise<CliSessionReport | null>;
//# sourceMappingURL=cli-session-collector.d.ts.map