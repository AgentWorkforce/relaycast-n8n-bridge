import type { RelayYamlConfig } from './types.js';
export declare const BUILT_IN_TEMPLATE_NAMES: readonly ["feature-dev", "bug-fix", "code-review", "security-audit", "refactor", "documentation", "review-loop"];
export type BuiltInTemplateName = (typeof BUILT_IN_TEMPLATE_NAMES)[number];
export interface TemplateRegistryOptions {
    builtInTemplatesDir?: string;
    customTemplatesDir?: string;
    workspaceDir?: string;
    fetcher?: typeof fetch;
}
export interface LoadTemplateOptions {
    overrides?: Record<string, unknown>;
}
export interface TemplateShorthandConfig {
    swarm: string;
    overrides?: Record<string, unknown>;
}
export type TemplateReferenceInput = string | RelayYamlConfig | (Partial<Omit<RelayYamlConfig, 'swarm'>> & TemplateShorthandConfig);
export declare class TemplateRegistry {
    private readonly builtInTemplatesDir;
    private readonly customTemplatesDir;
    private readonly fetcher;
    constructor(options?: TemplateRegistryOptions);
    listBuiltInTemplates(): string[];
    listCustomTemplates(): Promise<string[]>;
    listTemplates(): Promise<string[]>;
    hasTemplate(name: string): Promise<boolean>;
    loadTemplate(name: string, options?: LoadTemplateOptions): Promise<RelayYamlConfig>;
    resolveTemplateReference(input: TemplateReferenceInput, options?: LoadTemplateOptions): Promise<RelayYamlConfig>;
    applyOverrides(config: RelayYamlConfig, overrides: Record<string, unknown>): RelayYamlConfig;
    installExternalTemplate(url: string, name?: string): Promise<string>;
    private isTemplateShorthand;
    private mergeRelayConfig;
    private normalizeTemplateName;
    private resolveBuiltInTemplatesDir;
    private resolveTemplatePath;
    private findTemplatePath;
    private readTemplateFile;
    private normalizeLegacyTemplate;
    private normalizeLegacyStep;
    private validateRelayConfig;
    private setOverride;
    private setOnValue;
    private resolveArrayItemIndex;
    private safeReadDir;
    private cloneValue;
}
//# sourceMappingURL=templates.d.ts.map